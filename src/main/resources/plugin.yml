name: DespawnedItems
version: '${version}'
main: com.popupmc.despawneditems.DespawnedItems
description: Relocates despawning ground items into nearby containers, cookers, entities, or empty space instead of deleting them.
author: junebug12851
website: https://github.com/1fairyfox/papermc-despawned-items
api-version: '1.21'

commands:
  despi:
    description: Manage despawn locations and despawning
    usage: /despi
    permission: despi.use
    permission-message: You do not have permission to use despawned items
  recycle:
    description: Recycles the item in your hand
    usage: /recycle
    permission: recycle.use
    permission-message: You do not have permission to recycle

permissions:
  despi.use:
    description: Allows use of the /despi command
    default: true
  despi.elevated:
    description: Allows managing despawn locations owned by others and admin/test subcommands
    default: op
  recycle.use:
    description: Allows use of the /recycle command
    default: true
