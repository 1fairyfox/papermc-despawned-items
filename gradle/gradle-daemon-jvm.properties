# Pin the Gradle daemon to JDK 21 (provisioned via the foojay resolver) so every task —
# including detekt, which doesn't yet support running on JDK 25 — runs on the plugin's
# actual target JVM. Portable across machines and CI.
toolchainVersion=21
